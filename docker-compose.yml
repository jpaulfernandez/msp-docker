version: "3"
services:
  proxy:
    container_name: msp-proxy2
    image: jwilder/nginx-proxy:latest
    ports:
      - "80:80"
      - "443:443"      
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./web/vhosts.d/:/etc/nginx/conf.d
    links:
      - mspweb
    networks:
      - msp-network2
  mspweb:
    container_name: mspweb
    command: npm run start
    build: ./client
    volumes:
      - ./data:/app
      - '/app/node_modules'
    environment:
      VIRTUAL_HOST: ledger.local.mspfamilylending.com
    expose:
      - 5000
    tty: true
    networks:
        - msp-network2

networks:
    msp-network2:
        driver: bridge
